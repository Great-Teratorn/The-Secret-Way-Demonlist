import routes from './routes.js';

export const store = Vue.reactive({
    dark: JSON.parse(localStorage.getItem('dark')) || false,
    toggleDark() {
        this.dark = !this.dark;
        localStorage.setItem('dark', JSON.stringify(this.dark));
    },
});

const app = Vue.createApp({
    data: () => ({ store }),
});
const router = VueRouter.createRouter({
    history: VueRouter.createWebHashHistory(),
    routes,
});

app.use(router);

app.mount('#app');

/* PERSISTENT GLOBAL PREDICTIVE DISCOVERY MODULE */var globalDb = []; function loadDb() { var x1 = new XMLHttpRequest(); x1.open('GET', './data/list.json', true); x1.onload = function() { if (x1.status === 200) { try { JSON.parse(x1.responseText).forEach(function(l) { globalDb.push({ name: l.name, type: 'Level', route: '/list' }); if (l.author) globalDb.push({ name: l.author, type: 'Creator', route: '/list' }); if (l.verifier) globalDb.push({ name: l.verifier, type: 'Verifier', route: '/list' }); }); } catch(e){} } }; x1.send(); var x2 = new XMLHttpRequest(); x2.open('GET', './data/leaderboard.json', true); x2.onload = function() { if (x2.status === 200) { try { JSON.parse(x2.responseText).forEach(function(p) { globalDb.push({ name: p.name, type: 'Player', route: '/leaderboard' }); }); } catch(e){} } }; x2.send(); } loadDb();document.addEventListener('click', function(e) { if (e.target.closest('#welcomeSearchBtn')) { e.preventDefault(); var box = document.getElementById('welcomeSearchBox'); var inp = document.getElementById('levelSearch'); if (box && inp) { box.classList.toggle('active'); if (box.classList.contains('active')) { inp.focus(); } else { inp.value = ''; var dd = document.getElementById('searchDropdownMenu'); if (dd) dd.style.setProperty('display', 'none', 'important'); } } } if (e.target.closest('.suggestion-row')) { var row = e.target.closest('.suggestion-row'); localStorage.setItem('pendingListSearch', row.getAttribute('data-name')); var dd = document.getElementById('searchDropdownMenu'); if (dd) dd.style.setProperty('display', 'none', 'important'); var inp = document.getElementById('levelSearch'); if (inp) inp.value = ''; window.location.hash = row.getAttribute('data-route'); } });document.addEventListener('input', function(e) { if (e.target && e.target.id === 'levelSearch') { var dd = document.getElementById('searchDropdownMenu'); if (!dd) return; var raw = e.target.value.toLowerCase().trim(); if (raw.length < 2) { dd.style.setProperty('display', 'none', 'important'); return; } var filtered = globalDb.filter(function(item) { return item.name.toLowerCase().indexOf(raw) !== -1; }).filter(function(v, i, a) { return a.findIndex(function(t) { return t.name === v.name && t.type === v.type; }) === i; }).slice(0, 6); if (filtered.length === 0) { dd.style.setProperty('display', 'none', 'important'); return; } var html = ''; filtered.forEach(function(item) { html += '<div class="suggestion-row" data-name="' + item.name.replace(/"/g, '&quot;') + '" data-route="' + item.route + '"><span class="suggestion-name">' + item.name + '</span><span class="suggestion-type">' + item.type + '</span></div>'; }); dd.innerHTML = html; dd.style.setProperty('display', 'block', 'important'); } });document.addEventListener('keydown', function(e) { if (e.target && e.target.id === 'levelSearch' && e.key === 'Enter') { var query = e.target.value.trim(); if (query.length > 0) { localStorage.setItem('pendingListSearch', query); window.location.hash = '/list'; } } });function runIncomingSearch() { var pending = localStorage.getItem('pendingListSearch'); if (!pending) return; var query = pending.toLowerCase().trim(); localStorage.removeItem('pendingListSearch'); var attempts = 0; var interval = setInterval(function() { attempts++; var route = window.location.hash; var targetClass = route.includes('/leaderboard') ? '.player-cell, td, th, .player' : '.list button, .list-container a, .list a, [class*="item"] button'; var els = document.querySelectorAll(targetClass); if (els.length > 0) { clearInterval(interval); els.forEach(function(el) { if (el.textContent.toLowerCase().indexOf(query) !== -1) { el.style.setProperty('border', '2px dashed #00e676', 'important'); el.style.setProperty('background', 'rgba(0, 230, 118, 0.15)', 'important'); el.scrollIntoView({ behavior: 'smooth', block: 'center' }); if (typeof el.click === 'function') el.click(); } }); } if (attempts > 30) clearInterval(interval); }, 150); }window.addEventListener('hashchange', runIncomingSearch); window.addEventListener('load', runIncomingSearch);